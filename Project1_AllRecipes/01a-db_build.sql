/*
Step 1a - Build the Database

Use this template file as the starting point to create the database and its basic structure.

NOTE: psql doesn't allow for context switching in scripts, so you'll have to run the second half of this *inside* of the new database *after* it has been created.
*/

-- Create Database
CREATE DATABASE All_Recipes; 